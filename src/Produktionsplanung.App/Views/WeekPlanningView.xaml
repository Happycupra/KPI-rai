<UserControl x:Class="Produktionsplanung.App.Views.WeekPlanningView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource SurfaceBrush}">
    <UserControl.Resources>
        <Style x:Key="WeekCellText" TargetType="TextBlock">
            <Setter Property="TextWrapping" Value="Wrap" />
            <Setter Property="VerticalAlignment" Value="Top" />
            <Setter Property="Margin" Value="4" />
        </Style>
    </UserControl.Resources>

    <Grid Margin="28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,18">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Text="Wochenplanung" FontSize="30" FontWeight="Bold" Foreground="{StaticResource TextBrush}" />
                <TextBlock Text="{Binding WeekRangeText}" FontSize="16" Foreground="{StaticResource MutedTextBrush}" Margin="0,4,0,0" />
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <Button Content="‹" Width="38" Height="34" Command="{Binding PreviousWeekCommand}" Margin="0,0,8,0" />
                <Button Content="Diese Woche" Height="34" Padding="14,0" Command="{Binding CurrentWeekCommand}" Margin="0,0,8,0" />
                <DatePicker SelectedDate="{Binding WeekStart, Mode=TwoWay}" Width="145" Height="34" Margin="0,0,8,0" />
                <Button Content="›" Width="38" Height="34" Command="{Binding NextWeekCommand}" Margin="0,0,8,0" />
                <Button Content="PDF Wochenplan" Height="34" Padding="14,0" Margin="0,0,8,0" Click="ExportWeeklyPlanPdf_Click" />
                <Button Content="Aktualisieren" Height="34" Padding="14,0" Command="{Binding RefreshCommand}" />
            </StackPanel>
        </Grid>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <Border Background="{StaticResource CardBrush}" CornerRadius="12" Padding="18" Margin="0,0,0,16">
                    <Grid>
                        <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="Wochenaktionen" FontSize="19" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}" />
                            <TextBlock Text="Die Vorwoche kann nur in eine noch leere Zielwoche kopiert werden. Der PDF-Wochenplan fasst Produkt, Maschine, Schicht und Mitarbeiter kompakt auf 1–2 A4-Seiten zusammen."
                                       Foreground="{StaticResource MutedTextBrush}" TextWrapping="Wrap" Margin="0,4,0,0" />
                            <TextBlock Text="{Binding StatusMessage}" Foreground="{StaticResource DangerBrush}" TextWrapping="Wrap" Margin="0,8,0,0" />
                        </StackPanel>
                        <Button Grid.Column="1" Content="Vorwoche kopieren" Padding="18,8" Margin="18,0,0,0"
                                VerticalAlignment="Center" Command="{Binding CopyPreviousWeekCommand}" />
                    </Grid>
                </Border>

                <Border Background="{StaticResource CardBrush}" CornerRadius="12" Padding="18" Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="Auftragsabdeckung der Woche" FontSize="19" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}" />
                        <TextBlock Text="Jede Produktionsschicht eines mehrschichtigen Auftrags wird separat dargestellt und mit der tatsächlich eingeplanten Mannschaft verglichen."
                                   Foreground="{StaticResource MutedTextBrush}" Margin="0,4,0,10" />
                        <DataGrid ItemsSource="{Binding ProductionOrderCoverage}" AutoGenerateColumns="False" IsReadOnly="True"
                                  CanUserAddRows="False" HeadersVisibility="Column" MinHeight="170" MaxHeight="300">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Datum" Binding="{Binding Date, StringFormat=dd.MM.}" Width="70" />
                                <DataGridTextColumn Header="Auftrag" Binding="{Binding OrderNumber}" Width="90" />
                                <DataGridTextColumn Header="Produkt" Binding="{Binding Product}" Width="1.2*" />
                                <DataGridTextColumn Header="Arbeitsplatz" Binding="{Binding WorkstationName}" Width="1.1*" />
                                <DataGridTextColumn Header="Schicht" Binding="{Binding ShiftName}" Width="105" />
                                <DataGridTextColumn Header="Lauf" Binding="{Binding RunText}" Width="75" />
                                <DataGridTextColumn Header="Bedarf" Binding="{Binding RequiredStaff}" Width="60" />
                                <DataGridTextColumn Header="Geplant" Binding="{Binding PlannedStaff}" Width="65" />
                                <DataGridTextColumn Header="Diff." Binding="{Binding DifferenceText}" Width="55" />
                                <DataGridTextColumn Header="Status" Binding="{Binding CoverageStatus}" Width="105" />
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </Border>

                <Border Background="{StaticResource CardBrush}" CornerRadius="12" Padding="18" Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="Mitarbeiterübersicht" FontSize="19" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}" Margin="0,0,0,4" />
                        <TextBlock Text="Arbeitsplatz, Schicht und Zeit pro Mitarbeiter und Tag; Abwesenheiten werden direkt in der Zelle angezeigt."
                                   Foreground="{StaticResource MutedTextBrush}" Margin="0,0,0,10" />
                        <DataGrid ItemsSource="{Binding EmployeeRows}" AutoGenerateColumns="False" IsReadOnly="True"
                                  CanUserAddRows="False" HeadersVisibility="Column" MinHeight="310"
                                  HorizontalScrollBarVisibility="Auto">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Mitarbeiter" Binding="{Binding EmployeeName}" Width="150" ElementStyle="{StaticResource WeekCellText}" />
                                <DataGridTextColumn Header="Funktion" Binding="{Binding Role}" Width="120" ElementStyle="{StaticResource WeekCellText}" />
                                <DataGridTextColumn Header="Montag" Binding="{Binding Monday}" Width="150" ElementStyle="{StaticResource WeekCellText}" />
                                <DataGridTextColumn Header="Dienstag" Binding="{Binding Tuesday}" Width="150" ElementStyle="{StaticResource WeekCellText}" />
                                <DataGridTextColumn Header="Mittwoch" Binding="{Binding Wednesday}" Width="150" ElementStyle="{StaticResource WeekCellText}" />
                                <DataGridTextColumn Header="Donnerstag" Binding="{Binding Thursday}" Width="150" ElementStyle="{StaticResource WeekCellText}" />
                                <DataGridTextColumn Header="Freitag" Binding="{Binding Friday}" Width="150" ElementStyle="{StaticResource WeekCellText}" />
                                <DataGridTextColumn Header="Samstag" Binding="{Binding Saturday}" Width="150" ElementStyle="{StaticResource WeekCellText}" />
                                <DataGridTextColumn Header="Sonntag" Binding="{Binding Sunday}" Width="150" ElementStyle="{StaticResource WeekCellText}" />
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </Border>

                <Grid Margin="0,0,0,16">
                    <Grid.ColumnDefinitions><ColumnDefinition Width="1.25*"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                    <Border Background="{StaticResource CardBrush}" CornerRadius="12" Padding="18" Margin="0,0,16,0">
                        <StackPanel>
                            <TextBlock Text="Besetzung je Tag, Arbeitsplatz und Schicht" FontSize="19" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}" Margin="0,0,0,10" />
                            <DataGrid ItemsSource="{Binding StaffingRows}" AutoGenerateColumns="False" IsReadOnly="True"
                                      CanUserAddRows="False" HeadersVisibility="Column" MinHeight="240" MaxHeight="360">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Tag" Binding="{Binding DayText}" Width="90" />
                                    <DataGridTextColumn Header="Arbeitsplatz" Binding="{Binding WorkstationName}" Width="1.2*" />
                                    <DataGridTextColumn Header="Schicht" Binding="{Binding ShiftName}" Width="1.1*" />
                                    <DataGridTextColumn Header="Zeit" Binding="{Binding TimeText}" Width="95" />
                                    <DataGridTextColumn Header="Plan" Binding="{Binding PlannedStaff}" Width="55" />
                                    <DataGridTextColumn Header="Min" Binding="{Binding MinimumStaff}" Width="50" />
                                    <DataGridTextColumn Header="Opt" Binding="{Binding OptimalStaff}" Width="50" />
                                    <DataGridTextColumn Header="Max" Binding="{Binding MaximumStaff}" Width="50" />
                                    <DataGridTextColumn Header="Status" Binding="{Binding StatusText}" Width="105" />
                                </DataGrid.Columns>
                            </DataGrid>
                        </StackPanel>
                    </Border>

                    <Border Grid.Column="1" Background="{StaticResource CardBrush}" CornerRadius="12" Padding="18">
                        <StackPanel>
                            <TextBlock Text="Warnungen und Hinweise" FontSize="19" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}" Margin="0,0,0,10" />
                            <ItemsControl ItemsSource="{Binding Alerts}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="{StaticResource SubtleSurfaceBrush}" CornerRadius="8" Padding="10" Margin="0,0,0,6">
                                            <StackPanel>
                                                <TextBlock Text="{Binding Severity}" FontWeight="SemiBold" Foreground="{StaticResource DangerBrush}" />
                                                <TextBlock Text="{Binding Message}" TextWrapping="Wrap" Foreground="{StaticResource TextBrush}" Margin="0,2,0,0" />
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>
                </Grid>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
